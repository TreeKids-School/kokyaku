rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 全ユーザー（認証済み・匿名含む）に全権限を付与 (初期導入フェーズ)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
